- if admin?
  .admin-panel
    = link_to t('phrases.edit_resource', resource: t('events.singular')), edit_event_path(event)

p = event.title
p = event.description
p = event.agenda
p = event.started_at
p = event.finished_at

= link_to t('words.back'), events_path
